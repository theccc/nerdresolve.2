<%- model_class = Blog -%>
<div class="page-header">
  <h1><%=t '.title', :default => Blog %></h1>
</div>

<div class="fieldset">
  <dl>
      <dt class="label label-default"><%= model_class.human_attribute_name(:titulo) %></dt>
      <dd><pre class="prettyprint"><%= @blog.titulo %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:subtitulo) %></dt>
      <dd><pre class="prettyprint"><%= @blog.subtitulo %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:texto) %></dt>
      <dd><pre class="prettyprint"><%= @blog.texto %></pre></dd>
      <dt class="label label-default"><%= model_class.human_attribute_name(:tags) %></dt>
      <dd><pre class="prettyprint"><%= @blog.tags %></pre></dd>
  </dl>
</div>

<div class="form-group">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              blogs_path, :class => 'btn btn-default'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_blog_path(@blog), :class => 'btn btn-default' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              blog_path(@blog),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>

